// Queries
const Query = require('./queries/Query')
const Snap = require('./queries/Snap')
const User = require('./queries/User')

// Mutations
const Mutation = require('./mutations')

module.exports = { Query, Mutation, Snap, User }
